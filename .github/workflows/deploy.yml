name: Github Actions 실행해보기

on: 
  push:
    branches:
      - main

jobs:
  My-Deploy-job:
    runs-on: ubuntu-latest

    steps:
      - name: Hello World 출력하기
        run: echo "Hello World"
      
      - name: 여러 명령어 문장 작성법
        run: |
          echo "Good"
          echo "Morning"
          pwd